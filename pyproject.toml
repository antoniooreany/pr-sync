[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "automation-toolkit"
version = "0.1.0"
description = "Automation Toolkit for PRs, Releases, and Repo Hygiene"
authors = [
    {name = "antoniooreany", email = "antoniooreany@example.com"}
]
dependencies = []

[project.scripts]
pr-sync = "pr_sync.cli:main"
